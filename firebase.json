{
  "functions": {
    "source": "functions",
    "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"]
  },
  "hosting": {
    "public": "frontend/dist",
    "_comment_predeploy": "⚠ WITHOUT THIS, `firebase deploy --only hosting` ships whatever is already in frontend/dist, however stale, and reports success. `firebase deploy` rebuilds FUNCTIONS but never the Vite frontend. On 2026-09-09 the matcher hosting deploy shipped current code only because the frontend had been built by hand minutes earlier. Mirrors the hook games/beergame/firebase.json already has; --prefix points at frontend/ because the matcher's SPA lives there rather than at the repo root.",
    "predeploy": ["npm --prefix \"$PROJECT_DIR/frontend\" run build"],
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [{ "source": "**", "destination": "/index.html" }]
  },
  "firestore": { "rules": "firestore.rules", "indexes": "firestore.indexes.json" },
  "database": { "rules": "database.rules.json" },
  "emulators": {
    "auth": { "port": 9101 },
    "functions": { "port": 5005 },
    "firestore": { "port": 8082 },
    "database": { "port": 9002 },
    "hosting": { "port": 5006 },
    "ui": { "enabled": true, "port": 4002 },
    "singleProjectMode": true
  }
}
